# Claude Hub — Project Context

> This is the project-level context file. It is automatically loaded when Claude
> Code runs inside this repository. The global equivalent lives at
> `~/.claude/CLAUDE.md` and is generated by `install.sh`.

@context/engineering-principles.md
@context/python-standards.md
@context/typescript-standards.md
@context/aws-conventions.md
@context/git-conventions.md

## Repository Purpose

Single source of truth for Claude Code context, skills, MCP servers, evals,
and multi-agent workflows. Run `install.sh` to wire everything into `~/.claude`.

## Skills

Before starting any task that matches a trigger below, read the full SKILL.md
first — it contains patterns, checklists, and common mistakes to avoid.

| Skill | Triggers | File |
|-------|----------|------|
| `python-refactor` | refactor, extract, clean up, restructure Python | `skills/python-refactor/SKILL.md` |
| `write-tests` | write tests, add tests, pytest, coverage, unit test | `skills/write-tests/SKILL.md` |
| `aws-cdk-construct` | CDK, AWS infra, CloudFormation, stack, Fargate, Lambda | `skills/aws-cdk-construct/SKILL.md` |
| `temporal-workflow` | Temporal, durable workflow, activity, worker | `skills/temporal-workflow/SKILL.md` |
| `mcp-server` | MCP server, FastMCP, tool definition, benchmark MCP | `skills/mcp-server/SKILL.md` |
| `langgraph-agent` | LangGraph, StateGraph, multi-agent, agent node | `skills/langgraph-agent/SKILL.md` |
| `eval-harness` | eval, LLM judge, benchmark, dataset.jsonl, pass rate | `skills/eval-harness/SKILL.md` |

## MCP Server Standards

- Use `fastmcp` (Python) for all MCP server implementations.
- Every tool must have a clear docstring — this becomes the tool description.
- Every tool must have a corresponding benchmark and at least 5 eval cases.
- Servers run on stdio transport for local use, SSE for deployed use.

## When Unsure

Ask for clarification rather than assuming. State your assumptions explicitly
before implementing.
